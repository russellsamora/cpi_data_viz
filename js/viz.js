var currentInfo = null,
    globalInfo = null,
    vizContainer = null;

$(function() {

    currentInfo = $('.currentInfo');
    globalInfo = $('.globalInfo');
    vizContainer = $('.vizContainer');
});